CREATE TABLE [dbo].[Excluded_Investments]
(
	[ID]			INT	IDENTITY(1,1)	NOT NULL PRIMARY KEY,
	[INV_ID]		NVARCHAR(255) NOT NULL UNIQUE,
	[Description]	NVARCHAR(MAX)	NULL,
	[Created_On]	DATETIME	DEFAULT GETDATE() NOT NULL,
)
